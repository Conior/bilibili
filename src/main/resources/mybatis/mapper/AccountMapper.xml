<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wsj.bilibili.mapper.AccountMapper">
    <select id="queryAllAccount" resultType="AccountDTO">
        SELECT * FROM tbl_account
    </select>

    <select id="queryAccount" parameterType="AccountDTO" resultType="int">
        SELECT * FROM tbl_account where user_name = #{username} and pass_word = #{password}
    </select>

    <insert id="insertAccount" parameterType="AccountDTO">
        insert into tbl_account (user_name,pass_word,gender,email,phone)
        values (#{username},#{password},#{gender},#{email},#{phone})
    </insert>

    <update id="updateAccountById" parameterType="AccountDTO">
        update tbl_account set
            username = #{user_name},
            password = #{pass_word},
            gender = #{gender},
            email = #{email},
            phone = #{phone}
        where id = #{id}
    </update>

    <delete id="deleteAccountById" parameterType="int">
        delete from tbl_account where id = #{id}
    </delete>
</mapper>